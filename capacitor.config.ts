import { CapacitorConfig } from '@capacitor/cli';

const config: CapacitorConfig = {
  appId: 'ef.ph',
  appName: 'ef-ph',
  webDir: 'www',
  server: {
    androidScheme: 'https'
  }
};

export default config;
